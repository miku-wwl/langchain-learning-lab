# Optional environment overrides. Values are not read from this file automatically.
# By default, model_factory discovers the current Foundry Local URL and loaded IDs.
# LOCAL_LLM_BASE_URL=http://127.0.0.1:<current-port>/v1
# LOCAL_LLM_MODEL=<loaded-model-id>
